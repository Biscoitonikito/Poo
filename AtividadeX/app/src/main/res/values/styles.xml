<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAppBar</item>
        <item name="colorPrimaryDark">@color/colorAppBar</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorLine</item>
        <item name="colorControlActivated">@color/colorHintClick</item>
        <item name="android:textColorHint">@color/colorHint</item>
    </style>
</resources>
