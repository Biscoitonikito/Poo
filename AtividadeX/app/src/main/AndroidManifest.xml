<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.guilherme.atividadex">

    <application
        android:name=".dal.App"
        android:allowBackup="true"
        android:icon="@drawable/ic_login_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_login_background"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".activity.FormularioMedicamentoActivity"
            android:parentActivityName=".activity.ListaMedicamentoActivity"/>
        <activity
            android:name=".activity.CadastrarActivity"
            android:parentActivityName=".activity.LoginActivity" />
        <activity
            android:name=".activity.ListaMedicamentoActivity"
            android:parentActivityName=".activity.ListaMedicamentoActivity" />
        <activity android:name=".activity.LoginActivity" android:parentActivityName=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>